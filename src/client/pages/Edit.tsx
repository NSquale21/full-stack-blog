import * as React from 'react';
import EditForm from '../components/EditForm';

const Edit = () => {
  return (
		<main>
			<section>
				<EditForm />
			</section>
		</main>
	);
};

export default Edit;